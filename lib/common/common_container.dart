import 'package:flutter/material.dart';

Widget commonContainer(
        {double? height, double? width, Decoration? decoration}) =>
    Container(
      height: height,
      width: width,
      decoration: decoration,
    );
